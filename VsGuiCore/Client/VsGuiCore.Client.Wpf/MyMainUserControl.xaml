<UserControl x:Class="TestGuiCore.Client.Wpf.MyMainUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestGuiCore.Client.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="24"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="24"/>
        </Grid.RowDefinitions>
        <Menu Name="MainMenu"/>
        <!--<xcad:DockingManager x:Name="dockManager" Grid.Row="1"/>-->
        <ContentControl Grid.Row="1" Name="ContentWrapper"/>
        <StatusBar Name="StatusBar" Grid.Row="2">
            <StatusBarItem Content="Test"/>
        </StatusBar>
    </Grid>
</UserControl>
